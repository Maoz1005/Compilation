int main() {
    int x := 10;
    while (x < 10) {
        int y := 10;
        PrintInt(y);
    } // y 4 INIT
    // Split into cases
    // If we access y before typing int y  Well get an error during Semantic check
    // If we access y after typing int y  Previous y state was immediately destroyed
}
